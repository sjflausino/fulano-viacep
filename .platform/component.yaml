apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "fulano-viacep"
  description: "Artifacts generator via Spec."
  annotations:
    github.com/project-slug: sjflausino/fulano-viacep
    backstage.io/techdocs-ref: dir:..
    github.com/workflows: publish.yml,remove.yml
    kong-manager/service-name: app.fulano-viacep.80
    kong-manager/instance: cluster-vkdr-local   
spec:
  type: devops
  lifecycle: experimental
  owner: "group:default/admin"
  providesApis:
    - fulano-viacep
